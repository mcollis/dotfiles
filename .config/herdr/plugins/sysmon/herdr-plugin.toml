id = "local.sysmon"
name = "Sysmon"
version = "0.2.0"
min_herdr_version = "0.7.0"
description = "Pushes threshold-colored CPU/MEM tokens to the workspace labeled ~ only."
platforms = ["linux", "macos"]

[[startup]]
command = ["bash", "scripts/poll.sh", "--spawn"]
platforms = ["linux", "macos"]
